# Alternative PyAudio installation for cloud deployment
# Use this if the standard PyAudio fails to build

# Option 1: Try PyAudio with specific version
PyAudio==0.2.11

# Option 2: If PyAudio still fails, use alternative packages
# Uncomment these and comment out PyAudio above:
# pydub==0.25.1
# google-cloud-speech==2.21.0
# azure-cognitiveservices-speech==1.31.0
